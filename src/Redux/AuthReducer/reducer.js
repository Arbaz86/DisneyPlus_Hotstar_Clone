import * as types from "./actionTypes";

const initState = {
  isAuth: false,
  token: "",
};

export const reducer = (state = initState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
